spring:
  profiles:
    active: local
    group:
      local: db-local
      server: db-server
    include:
      - db
      - security
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/board_server?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=utf8
    username: urzor
    password: 5neoljeoneockoenyeon2boo
mybatis:
  mapper-locations: classpath:com.fastcampus.boardserver.mapper/*.xml